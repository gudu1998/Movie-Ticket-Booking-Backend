const eErrorMessage = {
    DbErrorMessage : 'Could not connect to the database. Exiting now...',
    FieldContent : 'Please enter the required fields!!',
    InvalidPassword : 'Invalid password!',
    InvalidEmail: "Email doesn't exist!"
};

Object.freeze(eErrorMessage);

export default  eErrorMessage;